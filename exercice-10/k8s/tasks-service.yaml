apiVersion: v1
kind: Service
metadata:
  name: tasks-service
  namespace: fullstack-ns
spec:
  selector:
    app: tasks-api
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: LoadBalancer